[package]
name = "wobotomite/sendbufs"
version = "1.1.3"
registry = "https://github.com/UpliftGames/wally-index"
realm = "shared"
description = "extensible networking lib with compression"
license = "MIT"
exclude = ["sendbufs.rbxmx", "pesde.toml", "pesde.lock", ".pesde", ".vscode", "rokit.toml", "lune_packages", "luau_packages", "sourcemap.json"]

[dependencies]
